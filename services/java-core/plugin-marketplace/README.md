# Plugin Marketplace — vertical slice (Phase 6.5)
- `services/java-core/plugin-marketplace` — registry, billing (KebuPay), reviews (Java)
- `services/go-gateway/plugin` — execution gateway, sandbox
- `packages/plugin-runtime-sdk` — manifest, permissions, CSP, sandbox attrs
- `apps/mfe-plugin-marketplace` — discovery/install/permissions/CSP + iframe sandbox (escape blocked)
