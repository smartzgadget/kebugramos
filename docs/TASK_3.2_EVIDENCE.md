# Task 3.2 — Evidence (mfe-kebuchat Workspace)

## Delivered
- `apps/mfe-kebuchat` — `contracts.ts` (ChatRow Zod + list response), `ChatList.tsx` (search field, filter chips, rows with business/verified badges + unread counter, bottom nav), `App.tsx` (320px chat list + right content panel, one showcase thread `KebuStore Support` with `isBusiness+isVerified+unread=2`, Figma topology: chat list panel → right panel).

## Validation
- Layout matches Figma §1: sovereign shell slot → chat list + content panel; badges/counters via `@kebugram/design-system`.

## Next
Task 3.3 — Message composer (attachments, voice, read receipts, typing).
