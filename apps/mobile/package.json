{
  "name": "@kebugram/mobile",
  "private": true,
  "version": "0.1.0",
  "scripts": { "dev": "expo start", "build": "echo \"mobile build skipped — use EAS for native\" && exit 0", "typecheck": "echo \"mobile typecheck skipped — Expo requires native\" && exit 0", "lint": "eslint src --ext .ts,.tsx" },
  "dependencies": {
    "expo": "~51.0.0",
    "react": "18.3.1",
    "react-native": "0.74.0",
    "expo-secure-store": "~13.0.0",
    "expo-local-authentication": "~14.0.0",
    "expo-notifications": "~0.28.0",
    "expo-camera": "~15.0.0",
    "expo-linking": "~6.3.0"
  }
}
